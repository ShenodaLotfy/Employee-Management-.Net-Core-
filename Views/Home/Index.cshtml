<!-- use _ViewImports to short the namespaces so we remove this long term namespace
    IEnumerable<NetCore_Model_View_Cortol_Created.Models.Employees> to IEnumerable<Employees>
    -->

@model IEnumerable<Employees>
<!-- define location of the _Layout.cshtml that has <html> <body> tags to be used in this view  -->
@{
    @*Layout = "~/Views/Shared/_Layout.cshtml"; we write it in _ViewStart.cshtml *@
    @* you can override _ViewStart configuration by setting Layout = "null"; *@
    ViewBag.Title = "Index";
}

@*<table>
        <thead>
            <tr>
                <td>Name</td>
                <td>Email</td>
                <td>Salary</td>
            </tr>
        </thead>
        <tbody>
            @foreach (var employee in Model)
            {
                <tr>
                    <td>@employee.name</td>
                    <td>@employee.email</td>
                    <td>@employee.salary</td>
                </tr>
            }
        </tbody>
    </table>*@

<div class="card-deck">
    @foreach (var employee in Model)
    {
        var photoPath = "~/images/" + (employee.PhotoPath ?? "noname.jpg");
        <div class="card m-3" style="min-width: 18rem; max-width:30.5%;">
            <div class="card-header">
                <h3>@employee.name</h3>
            </div>

            <img class="card-img-top" src="@photoPath" asp-append-version="true" />
            <div class="card-footer text-center">
                <a asp-controller="Home" asp-action="details" asp-route-id="@employee.id" class="btn btn-primary m-1">View</a>
                <a asp-controller="Home" asp-action="edit" asp-route-id="@employee.id" class="btn btn-primary m-1">Edit</a>
                <a href="#" class="btn btn-danger m-1">Delete</a>
            </div>
        </div>
    }
</div>

<!-- define script section to be available in _Layout.cshtml -->
@section Scripts{
    <script src="~/js/CustomScript.js"></script>
}
